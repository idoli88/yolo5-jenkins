pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'echo building...'
                sh 'echo building...2' // this is a duplicate
            }
        }
    }
}